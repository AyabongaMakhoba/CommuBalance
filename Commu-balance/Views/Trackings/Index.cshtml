@model IEnumerable<Commu_balance.Models.Tracking>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Delivery.Delivery_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Track_Message)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Delivery_Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Delivery.Delivery_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Track_Message)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Delivery_Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Track_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Track_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Track_ID })
        </td>
    </tr>
}

</table>
