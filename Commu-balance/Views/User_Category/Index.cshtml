@model IEnumerable<Commu_balance.Models.User_Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Profile.User_IDNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Profile.User_IDNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserCat_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserCat_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserCat_ID })
        </td>
    </tr>
}

</table>
