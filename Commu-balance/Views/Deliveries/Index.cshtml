@model IEnumerable<Commu_balance.Models.Delivery>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Schedule.Driver_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Delivery_Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Schedule.Driver_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Delivery_Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Delivery_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Delivery_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Delivery_ID })
        </td>
    </tr>
}

</table>
