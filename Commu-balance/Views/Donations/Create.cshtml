@model Commu_balance.Models.Donation

@{
    ViewBag.Title = "Donations";
}

<h2>Make Donation</h2>
@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(model => model.Donation_Amt, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.EditorFor(model => model.Donation_Amt, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Donation_Amt, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Procced To Payment" class="btn btn-success" />
        </div>
    </div>
    <br />
    <div class="row">
        <a href='@Url.Action("Index", "Donations")' class="btn btn-warning">Previous Donations</a>

        @*<a href='@Url.Action("Edit", "Quotes", new { id = Model.Quote_ID }, null)' class="btn btn-warning">Edit</a>

            <a href='@Url.Action("Delete", "Quotes", new { id = Model.Quote_ID }, null)' class="btn btn-secondary">Cancel</a>*@
    </div>
    <br />

    <div>
        @{
            Html.RenderAction("PartialIndex", "Donations");
        }
    </div>
</div>
            }

                @section Scripts {
                    @Scripts.Render("~/bundles/jqueryval")
                }
